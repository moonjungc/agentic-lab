# GOLDEN RULES

절대 위반 금지 규칙:

1. 활성 결제 코드는 `src/payments/`뿐이다. `src/billing/`, `src/old/`는 DEPRECATED — 수정도, 참조도 하지 않는다.
   - Why: 두 폴더는 과거 결제 시스템의 잔재(원 단위 금액, 콜백 스타일 API)로, 현재 규칙과 다른 방식으로 금액을 계산해 섞어 쓰면 정산 오류로 이어진다.
2. 금액은 항상 전(minor unit, 1/100원) 정수로 다룬다.
   - Why: 원 단위 부동소수점 계산은 반올림 오차가 누적되어 결제 금액이 실제 청구액과 어긋나는 사고를 낳는다.
3. 결제 관련 로그는 `lib/logger.js`의 `logPayment()`만 사용한다.
   - Why: 정산·감사 시스템이 `ts/event/order_id/amount_minor/currency/status` 고정 스키마를 전제로 로그를 읽는다. `console.log`를 직접 쓰면 스키마가 흐트러져 파싱이 깨진다.
4. 코드 변경 후에는 반드시 `npm test`를 실행해 통과를 확인한다.
   - Why: 수수료 내림(버림) 계산처럼 반올림 방식만 살짝 틀려도 금액이 어긋나는 로직이 있어, 리팩터링 시 회귀를 놓치기 쉽다.
5. 환불 등 결제 세부 규칙은 @docs/payment-rules.md를 따른다.
   - Why: 수수료율·검증 조건 같은 도메인 규칙은 자주 바뀌므로 골든룰이 아닌 별도 문서에서 관리해 갱신 부담을 낮춘다.
